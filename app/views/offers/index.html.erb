<h1 style="text-align: center;">Upcoming Swaps</h1>
<% @offers.each do |offer| %>
  <%#= offer.id %>
  <%#= offer.user.email %>
   <%= render 'shared/chat_card', offer: offer  %>
  <%#= link_to "", request_offer_messages_path(offer.id, offer.request_id), class:"fas fa-comment-alt" %>
  <% end %>
